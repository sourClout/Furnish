<Window x:Class="Furnish.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" 
        Title="MainWindow" Height="450" Width="600" ResizeMode="NoResize" WindowStyle="SingleBorderWindow">
    <DockPanel>
        <Border DockPanel.Dock="Top" BorderBrush="Gray" BorderThickness="0.5" Background="#FF8522Bd"  >
            <Menu>
                <MenuItem Header="_File" Foreground="White">
                    <MenuItem Header="_Export"/>
                    <MenuItem Header="_Open"/>
                    <MenuItem Header="_Save"/>
                    <Separator/>
                    <MenuItem Header="_Exit"/>
                </MenuItem>
            </Menu>

        </Border>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Name="TbkStatus"/>
            </StatusBarItem>
        </StatusBar>
        <Border DockPanel.Dock="Left" BorderBrush="Gray" BorderThickness="0.5" Background="Black">
            <Grid Width="170" Margin="0,0,0,0" Background="#FFF1F0F3">
                <Button Name="BtnCustomers" HorizontalAlignment="Left" Margin="14,70,0,0" VerticalAlignment="Top" Width="140" Click="BtnCustomers_Click">
                    <Grid Width="130">
                        <materialDesign:PackIcon Kind="Account" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Center" Text="CUSTOMERS" />
                    </Grid>
                </Button>
                <Button Name="BtnProducts" HorizontalAlignment="Left" Margin="14,137,0,0" VerticalAlignment="Top" Width="140" Click="BtnProducts_Click">
                    <Grid Width="130">
                        <materialDesign:PackIcon Kind="TableFurniture" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Center" Text="PRODUCTS" />
                    </Grid>
                </Button>
                <Button  Name="BtnOrders" HorizontalAlignment="Left" Margin="14,210,0,0" VerticalAlignment="Top" Width="140" Click="BtnOrders_Click">
                    <Grid Width="130">
                        <materialDesign:PackIcon Kind="Invoice" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Center" Text="ORDERS" />
                    </Grid>
                </Button>



            </Grid>
        </Border>
        <Border DockPanel.Dock="right" BorderBrush="Gray" BorderThickness="0.5" Background="Black">
            <Grid Margin="0,0,0,0" Background="#FFF1F0F3">
                <TextBox x:Name="TbxUserName" TextWrapping="Wrap" Text="TbxUserName" Width="100" Height="28" Margin="47,36,281,0" VerticalAlignment="Top" TextChanged="TbxUserName_TextChanged"/>
                <TextBox x:Name="TbxEmail" TextWrapping="Wrap" Text="TbxEmail" Width="100" Height="28" Margin="47,82,281,0" VerticalAlignment="Top" TextChanged="TbxUserName_TextChanged"/>
                <Button  Name="BtnLogin" HorizontalAlignment="Left" Margin="269,263,0,0" VerticalAlignment="Top" Width="140" Click="BtnLogin_Click">
                    <Grid Width="130">
                        <materialDesign:PackIcon Kind="Invoice" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Center" Text="Log in" />
                    </Grid>
                </Button>
                <Button  Name="BtnRegister" HorizontalAlignment="Left" Margin="269,311,0,0" VerticalAlignment="Top" Width="140" Click="BtnRegister_Click">
                    <Grid Width="130">
                        <materialDesign:PackIcon Kind="Invoice" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Center" Text="Register" />
                    </Grid>
                </Button>
            </Grid>
        </Border>




    </DockPanel>
</Window>